server:
  port: 8889

logging:
  path: logs
  file: mylog.log

spring:
  application:
    name: myTest
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mytest?useCharactorEncoding=utf-8&useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8
    username: root
    password: password

mybatis:
  type-aliases-package: com.pss.model
  mapper-locations:
    - mapper/*
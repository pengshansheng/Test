server:
   port: 8888

logging:
    path: logs
    file: mylog.log


spring:
   application:
          name: report
   datasource:
       driver-class-name: com.mysql.jdbc.Driver
       url: "jdbc:mysql://localhost:3306/mytest?useCharactorEncoding=utf-8\
     &useSSL=false&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8"
       username: root
       password: password

mybatis:
    type-aliases-package: com.course.model
    mapper-locations:
       - mapper/*
